import { cn } from "@/lib/utils";
import { roleLabel } from "@/lib/access";

export function InitialsCircle({ member, size = "md", active = false, className }: { member: any; size?: "sm" | "md" | "lg"; active?: boolean; className?: string }) { const sizes = { sm: "h-6 w-6 text-[8px]", md: "h-9 w-9 text-[10px]", lg: "h-12 w-12 text-xs" }; const initials = member?.initials || member?.displayName?.split(" ").map((word: string) => word[0]).join("").slice(0, 3).toUpperCase() || "—"; return <span className={cn("relative inline-flex shrink-0", className)} title={member?.displayName || "Team member"}><span className={cn("flex items-center justify-center rounded-full font-bold tracking-[0.05em] text-white shadow-sm", sizes[size])} style={{ backgroundColor: member?.colour || "#5E7B70" }}>{initials}</span>{active ? <span aria-label="Active now" className="absolute -bottom-0.5 -right-0.5 h-2.5 w-2.5 rounded-full border-2 border-white bg-[#46A76B]" /> : null}</span>; }

export function ColleagueMarker({ member, active = false, showRole = false, className }: { member: any; active?: boolean; showRole?: boolean; className?: string }) { if (!member) return <span className="text-xs text-[#9AA39F]">Unassigned</span>; return <span className={cn("inline-flex items-center gap-2 text-xs text-[#5E6D65]", className)}><InitialsCircle member={member} size="sm" active={active} /><span className="min-w-0"><span className="block truncate">{member.displayName}</span>{showRole && member.memberRole ? <span className="block truncate text-[10px] text-[#87928C]">{roleLabel(member.memberRole)}</span> : null}</span></span>; }
