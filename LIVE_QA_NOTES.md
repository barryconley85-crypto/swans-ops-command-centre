# Live acceptance notes

## 27 August 2026

- Verified that the Vercel-hosted workspace loads and the `bc@swanstravel.com` lead account can sign in to the production dashboard.
- Confirmed the clean command board, navigation, and operations-lead controls are visible in the authenticated production session.
- Found that a newly created invitation did not appear in the active team directory before first sign-in, preventing immediate task and rota assignment. A correction to merge lead-visible invitations into the operational team view was prepared, type-checked, and pushed to the linked GitHub repository.
- Further workflow tests will resume after the latest Vercel deployment and matching Firestore policy are active.
- The Firebase console was available for the original policy publication but is currently rendering an empty main panel after navigation; the repository rule update is committed and awaits the matching live-editor update before a non-lead task-completion test is performed.
- Reopening Firestore through its navigation restored the console shell, but the Rules editor remained in a loading state immediately after navigation. No live policy changes were made during this interval.

## Completed production acceptance results

- The linked Vercel production release was retested after each correction. The latest verified application commit is `1c4685e` (`Show task owners on command board`).
- **Invited-team planning:** the disposable invited profile was visible in the team directory and became selectable in task, rota, handover, issue, and performance-note owner selectors before first sign-in. The profile was then removed as part of QA cleanup.
- **Daily tasks and checklist evidence:** a QA task assigned to the invited profile was created, moved through pending → in progress → complete, received a dated comment, and updated the task-board completion metric. A labelled two-item reusable checklist was saved, applied to today, and each generated task was completed successfully.
- **Rota and on-call:** a disposable core shift and on-call assignment were persisted for the invited profile. Coverage-gap signalling and shift/on-call distribution counts updated correctly. Initial on-call persistence failed because optional times were passed to Firestore as `undefined`; the shared write helper now omits undefined fields, its regression test passes, and the on-call retest succeeded.
- **Handovers:** a labelled high-priority handover with owner and decision record was created, acknowledged, resolved, found under its status filters, and reflected in corrected lifecycle counters. The counter defect was fixed by maintaining an all-records subscription separate from the selected register view.
- **Issues:** a labelled low-impact QA issue was created, moved to monitoring, and resolved with a resolution record. Native browser prompts can delay automation of the optional resolution text; a controlled prompt value confirmed that the underlying resolution write and resolved filter work.
- **Readiness:** the lead submitted a green pulse, then a same-day amber update. The result remained one of two team pulses (rather than duplicating), while the clear/watch counters and note content updated correctly.
- **Performance:** task completion, timeliness, shift and on-call counts rendered on the performance view, and a labelled QA coaching-note write was displayed successfully.
- **Command board:** task owner display was corrected after live QA found the dashboard reading `task.member` while the adapter supplied `task.assignee`. The adapter now provides a consistent member alias. The repaired board shows the task owner and on-call lead correctly.
- **Mobile:** authenticated production routes for dashboard, tasks, rota, and handovers were captured at a true 390 × 844 CSS-pixel viewport. All key controls and panels remained usable without overlap. The rota preserves a deliberate horizontally scrollable seven-day grid.
- **Validation:** `pnpm check` completed successfully. `pnpm test -- --run` completed successfully with four test files and six tests, including the new Firestore optional-field regression test.
- **Cleanup:** all user-visible, explicitly labelled QA records were deleted successfully (HTTP 200 for three tasks, two rota records, one handover, one issue, one readiness pulse, one performance note, one template, and the QA invitation). The production workspace was then visually confirmed clean. Immutable task-activity records remain orphaned by design because production rules prohibit their deletion; they are not displayed without their deleted task.

## Remaining rollout consideration

- The app’s post-invitation self-service task-update rule, which matches a signed-in team member by `assignedEmail`, has not been tested with a real invited colleague completing first-time sign-in. A fresh real work-email invitation should be used for that operational onboarding check.
- Direct client Firestore rules intentionally allow members to update handovers and issues for this small internal workspace. Before broader rollout, this should be hardened with field-level ownership constraints or trusted server-side endpoints.

## Collaborative operations release

- **Team chat:** the new authenticated operations channel loaded in production after its Firestore policy was published. A clearly labelled QA message was posted and rendered successfully.
- **On-call portal:** a high-priority QA item was logged with a named follow-up owner, acknowledged, resolved with closure evidence, and displayed against the named on-call lead sourced from the rota.
- **Task alerts:** assigning a QA task to the lead created both a live pop-up and a retained recipient-specific notification. The notification could be marked read. A live request also confirmed the optional server-side email endpoint returns `202 not_configured` safely when no external email sender has been set up.
- **Rota:** the standard shift-pattern controls display the agreed 06:00–15:00, 07:00–16:00 and 09:00–18:00 operating patterns. A QA on-call assignment appeared in a dedicated highlighted grid cell and drove the on-call portal’s coverage card.
- **Mobile retest:** an initial mobile-capture recovery state was traced to a temporary mismatch between the already-published recipient-only notification rule and the un-deployed filtered subscription. After the matched production release was ready, the 390 × 844 authenticated captures for both team chat and the on-call portal loaded successfully with no runtime events.
- **Cleanup:** one QA chat message, on-call item, on-call rota record, assigned task and notification were removed after validation (all returned HTTP 200). The production workspace was left clear of this release’s test data.

## Rota repair and end-of-day report preparation

- **Rota controls:** production commit `38c9281` was checked as the authenticated operations lead. Each duty now exposes persistent, touch-friendly Edit and Remove controls rather than relying on a hover-only button. The Edit dialog pre-populates the date, person, duty, times and cover note. Removal now uses an in-app explicit confirmation dialog, avoiding unreliable browser-native prompts.
- **Duplicate correction:** the two Friday 4 September 2026 Barry Conley Early entries were each inspected. Both showed the same date, team member, `06:00–15:00` times and no cover note. One record was removed through the live confirmation dialog. The selected week now shows exactly one Early duty on Friday 4 September; the separate Friday 28 August duty remains in place.
- **Duplicate safeguard:** both the rota form and the underlying rota create/update mutation now reject the same person, date, duty type and start/end time while allowing a different duty for the same person on the same date. Regression coverage covers this exact comparison.
- **End-of-day endpoint:** production commit `ffee03e` introduced `/api/end-of-day-report`. An unauthenticated live request correctly returned `Unauthorised report trigger`; it did not expose task data or send email. The protected route is ready for a free external 17:00 Europe/London scheduler after its server-only Firebase service account, scheduler secret and Resend sender credentials are configured.
- **Validation:** `pnpm check`, `pnpm test -- --run` (five files, eleven tests) and `pnpm build` passed for both commits. The existing Vite bundle-size advisory remains non-blocking.

## Reports, access and quick on-call release

- **Report centre:** production commit `6ee6a80` added `/reports`, verified live as the operations lead. The catalogue includes leadership risk, daily control, outstanding/overdue and blocked work, completion-evidence exceptions, recorded contributions, low recorded activity, rota/on-call coverage, holiday/availability, duty distribution, on-call follow-up and closure, handover/issue ageing, readiness gaps and data-quality prompts. The displayed information comes only from live Command Centre records and each view includes an operational-context caveat.
- **Coverage and absence:** the live coverage report correctly rendered dates missing Early, Core, Late or On-call coverage. The rota Add cover form now displays Holiday alongside the existing duty, leave and unavailable types, treats it as time-free and was closed without saving a test absence record.
- **Superuser and roles:** `bc@swanstravel.com` remains the lead/superuser. The lead can add colleagues, edit role/profile details, deactivate access while retaining historical records, and cancel a pending invitation. Supported levels are Operations manager, Coordinator, On-call responder and Viewer; matching Firestore rules were compiled and published successfully. No existing team profile or role was changed during release QA.
- **Quick on-call:** `/quick-on-call` loaded as a standalone authenticated route, separate from the command-centre sidebar. At 390 × 844, the rapid issue type, priority, headline, essential details, optional owner and next-action fields, as well as the primary submit button, were reachable without overlap or horizontal scrolling. No test on-call issue was submitted.
- **Validation:** `pnpm check`, `pnpm test -- --run` (six files, fourteen tests), `pnpm build`, `git diff --check` and Firestore rule compilation all passed. The 390 × 844 report-centre mobile capture retained readable report selection, date controls and horizontally scrollable result tables. The Vite bundle-size advisory remains non-blocking.

## Report customisation and role-completion update

- **Lead-owned report customisation:** the report centre now includes superuser-only colleague and task-status filters, carries these choices into saved report views, and retains CSV export for the selected result set. Other roles can run the standard live reports but cannot create saved report configurations or use lead-only filters.
- **Holiday-conflict insight:** the report catalogue now includes Holiday-related coverage risk, which identifies holiday, leave or unavailable entries that coincide with any missing Early, Core, Late or On-call cover. This is distinct from the full holiday calendar and generic coverage views.
- **Role completion:** Dispatcher is now an explicit selectable access level. Managers may maintain tasks, rota and templates; Coordinators and Dispatchers can maintain shared handovers/issues; On-call responders have the fast on-call workflow; Viewers remain read-only; and lead-only controls continue to cover staff lifecycle, saved report views and permanent operating controls. Updated rules compiled and were published successfully.
- **Quick on-call status:** the standalone phone route now displays up to three unresolved on-call follow-ups with their current lifecycle status and next action, or clearly confirms when no unresolved follow-ups are recorded.

## Team-adoption workflow release

- **My shift and support:** production commit `4443443` added an authenticated `/my-shift` view showing a colleague’s own rota duty, unfinished assigned work, relevant open handovers and support requests, alongside a short help/cover request form. `/help` provides the shared request board with open, acknowledged, resolved and all-request views. No test support request was created in the live workspace.
- **Duty-aware start and close:** `/shift-check` now presents short Start shift and Close shift paths. Both record explicit handover review and optional risk context; the close path also captures an optional named next owner and next action. The prompt is limited to one record per person, date and phase, so it is a concise operating aid rather than repetitive administration.
- **Lead operational signals:** the Today board now gives the lead an operational-signal card for missing Early/Core/Late/On-call cover, unowned work, unowned support requests and unrecorded readiness pulses. It explicitly frames these as missing inputs for coordination, not staff performance scoring.
- **Mobile and validation:** the My shift and Help & cover routes were captured at 390 × 844 and retained readable, reachable controls with no horizontal overflow. TypeScript checking, the production build and eleven Vitest files (twenty-two tests) passed. The Firestore policy compiled and was published successfully. The existing Vite bundle-size advisory remains non-blocking.
- **Duty-specific completion:** production commits `1e442ec` and `a199f8c` added the duty-aware essentials and live entry point. On the final production route, the Start shift prompt correctly rendered the current On-call duty, its specific two-item essentials, explicit handover-review checkbox, risk capture, and Record shift start action. At 390 × 844, those controls remained visible and reachable without clipping or horizontal overflow. There were no live open handovers at the time of verification, so no acknowledgement write was created solely for QA; the direct acknowledgement action appears automatically when an open handover exists and is restricted by the established shared-editor policy.

## Colleague identity and active-now release

- **Self-service identity:** production commit `ad69190` added `/profile`, reachable through the signed-in account menu as **My initials & colour**. The live lead profile showed its current `BC` marker, eight accessible preset colours and a custom colour picker. No initials, colour, role or other live team profile field was changed during validation.
- **Presence:** the sidebar now shows an **Active now** strip using coloured initials with a green activity dot. In the authenticated production check, the current Barry Conley session appeared as the one active colleague. Presence is based only on recent workspace activity and naturally expires after two minutes; it does not collect location, screen time or productivity data.
- **Responsibility markers:** the same initials-and-colour pattern is used in task detail, task ownership, handover ownership, the on-call follow-up owner and the existing rota/team displays, making responsibility recognisable without relying on a person’s full name alone.
- **Mobile:** at 390 × 844, the profile screen retained a clear initials input, visible colour choices, single-tap save action and the privacy explanation. No clipping or horizontal overflow was observed.
- **Validation:** TypeScript checking, the production build, Firestore rule compilation/deployment and twelve Vitest files (twenty-three tests) passed. The pre-existing Vite bundle-size advisory remains non-blocking.
- **Coverage completion:** the shared marker now includes initials, chosen colour, name and role when the card/row has space. It was applied to rota/duty rows, task ownership and task detail, handover ownership, issue ownership, on-call follow-up owners and help/cover requester and owner context. The current live workspace has no task, handover, issue, on-call or help-request card requiring an owner at the time of testing, so no artificial record was created solely to populate a marker. The production code compiled cleanly with each marker-bearing view, while the populated rota was checked on desktop and phone.
- **Cross-screen QA:** final 390 × 844 captures confirmed the profile selector, rota ownership rows, task board and issue-register empty state remain readable, operate without horizontal viewport overflow and do not conceal the new controls. Desktop production verification confirmed the Active now strip and the populated rota role marker (`BC`, Barry Conley, Lead / superuser). The absence of live task/issue/support-card data is reflected above rather than represented with fabricated examples.
- **Populated ownership check and cleanup:** a single clearly labelled task, `QA marker validation — remove after check`, was temporarily assigned to Barry Conley in production to verify the populated task marker. The live task card displayed the coloured `BC` circle and owner name alongside status and priority. The record was then located by its exact QA title through the authenticated session and deleted; the cleanup response confirmed `matched: 1, deleted: 1`. No other task or operational record was modified.
- **Final populated ownership QA:** the remaining owner contexts were verified with one clearly labelled temporary record each. The populated desktop handover showed the `BC` marker and Barry Conley owner label. The issue owner block showed `BC`, Barry Conley and `Lead / superuser`; the on-call follow-up owner showed `BC` and Barry Conley; and the Help & cover request showed matching populated requester and owner blocks, each with `BC`, Barry Conley and `Lead / superuser` after acknowledgement.
- **Final phone QA:** true 390 × 844 authenticated captures confirmed populated marker cards for the handover, issue and on-call views. The BC circles and owner labels remained legible, with context, controls and card edges contained cleanly. Because acknowledging the Help & cover request removes it from the default Open filter, a targeted capture using the Acknowledged filter was also taken; it visibly retained both populated requester and owner markers without clipping or horizontal overflow.
- **Final cleanup:** the four temporary production records — `QA marker validation — handover`, `QA marker validation — issue`, `QA marker validation — on-call` and `QA marker validation — support` — were removed by exact-title queries in their respective Firestore collections. Follow-up verification returned zero remaining matches for all four titles. The one retained notification whose body exactly matched the earlier deleted QA task was also removed; unrelated notifications and records were not touched.
- **Mobile task completion and cleanup:** one final clearly labelled task, `QA marker validation — mobile task`, was assigned to Barry Conley solely to fill the mobile owner context. In the true 390 × 844 capture, the coloured BC circle and Barry Conley label remained legible within the task card alongside its compact note, status, priority and Start action, with no horizontal overflow. The exact-title cleanup then deleted one task and one notification whose body exactly matched that QA task. Follow-up checks returned zero matching tasks and zero matching notifications. Together with the earlier task, rota, handover, issue, on-call and Help & cover checks, this completes populated desktop/mobile marker coverage for every ownership workflow.

## Live health check and Help & cover entry correction

- **Route health:** the authenticated production dashboard, profile, My shift, shift check, tasks, rota, quick on-call, on-call portal, Help & cover, handovers, issues, readiness, reports, chat and people routes were each opened from clean browser targets after the release. Every route rendered its intended primary heading and meaningful page content, had no detected horizontal viewport overflow and produced no page console errors. This was read-only route verification; no additional operational records were created.
- **Production runtime:** Vercel reported no grouped runtime error clusters for the project in the preceding 24-hour window.
- **Confirmed usability defect:** Help & cover originally showed the request register and its status filter but no visible way to create a request; the only entry was the less-obvious Ask for help dialog on My shift. Production commit `478e772` adds a prominent **Request help or cover** action directly to the shared register, reusing the same structured request fields and permission checks. The empty state now points to this visible action rather than away from the page.
- **Live confirmation:** as the authenticated operations lead, the new action was visible and opened the request-type, urgency, headline and support-context form successfully. It was closed without submitting data. A 390 × 844 mobile check confirmed the control, dialog and six form controls were present with no horizontal overflow. Viewer-level colleagues see the action disabled, in line with existing write permissions.
- **Validation:** `pnpm check`, `pnpm test -- --run` and `pnpm build` passed after the correction. The suite now has twelve test files and twenty-four tests, including the direct shared-register help-request payload test. `git diff --check` also passed. The pre-existing Vite bundle-size advisory remains non-blocking.
- **Mobile cross-workflow audit:** a second authenticated production pass opened all fifteen primary routes at a true 390 × 844 viewport. Every route rendered its expected heading, remained within the viewport, exposed visible controls and produced no browser console errors. The My shift help dialog, Add task dialog, on-call item dialog, direct Help & cover dialog and issue-log dialog were each opened and closed without submission; all mounted their expected controls and created no data. The standalone quick-on-call form and the remaining operational views also loaded successfully. The initial test-helper dialog observations were timing false positives caused by measuring before the animated dialog mounted; after a 300ms mount wait, the repeat audit returned zero route or interaction failures.
- **End-to-end operational checks:** clearly labelled, customer-free temporary records exercised the production create-to-resolution paths for Help & cover, handovers, issues and on-call. Help & cover moved through Open, Acknowledged and Resolved with named owner context; the handover moved through Open, Acknowledged and Resolved; the issue moved from Open to Monitoring to Resolved; and the on-call item moved from Open to Acknowledged to Resolved with resolution evidence visible. The issue Resolve control’s optional browser prompt required a QA-only response in the automation session; after that response was supplied, the normal control completed as expected.
- **Additional core workflow checks:** an owned task was created through the task board and rendered with its owner marker before deletion; no task-state action was taken, so no task activity was created. After explicit user approval, one temporary labelled Team chat message was sent using Ctrl/⌘ + Enter, displayed with initials and timestamp, then deleted. Because no genuine lead readiness pulse existed today, a temporary green readiness pulse was submitted and visibly updated the count and capacity card from 0 of 1 to 1 of 1 before deletion. The rota loaded its current cover, gap and distribution calculations, and the Add cover form plus edit/remove controls were available; the full create/edit/remove flow had already been independently verified during the prior duplicate-duty repair.
- **Final temporary-data cleanup:** exact-match verification returned zero remaining QA records in `ops_help_requests`, `ops_handovers`, `ops_issues`, `ops_on_call_items`, `ops_tasks`, `ops_chat_messages` and `ops_readiness`. All seven temporary records were removed. A task-notification match was also checked: no matching notification document was present, so no unrelated notification was changed.
- **Remaining safe controls:** My shift correctly showed the lead’s current on-call duty, personal work summary, support path and Start / close check link. The standalone Quick on-call page loaded with its short incident form, owner selector, next-action field and link back to the full board. The Report centre loaded all report options, date/person/status filters and the current leadership-risk results; export and saved-view controls were visible but not invoked, avoiding new output or saved data. Team & performance loaded the live directory, contribution table, Add note and Add team member management controls; no personnel record was changed. The earlier true 390 × 844 route audit also loaded Shift check with its expected heading and visible controls. These safe-control reviews, together with the complete mutable-workflow checks above, found no additional product defect.

## Google-owned daily report activation

- **Consent and live-data preview:** on 28 August 2026, the owner completed the Google consent flow for the private Apps Script project **Swans Operations — Daily 17:00 Report**. The safe `previewEndOfDayReport` run completed at 01:19:49 and generated the `2026-08-28` live-data report with 0 completed and 0 outstanding tasks. It did not send email or write the date-specific sent marker.
- **Schedule:** `installDaily17Report` completed at 01:22:53. The trigger register was directly inspected and showed exactly one owner-owned time-based trigger for `sendEndOfDayReport`. Its inspection dialog showed the daily 5:00–6:00 p.m. scheduling window. No manual test email was sent, so the initial scheduled delivery has not yet been mailbox-verified.
- **Operational boundary:** the recipient in the script is `bc@swanstravel.com`. The automation is owned by `barryconley85@gmail.com`, uses the owner Google account’s approved Apps Script permissions, and requires no Vercel environment variable, Firebase service-account JSON, or mail-provider key in the application repository.

## Manual readiness-pulse retirement

- **Operating decision:** the separate capacity/check-in flow was retired because it would create unnecessary daily administration for a small, busy operations team. Daily coordination now relies on information already required to operate the service: assigned and overdue tasks, rota/on-call cover, handovers, issues and Help & cover requests.
- **Implementation:** Readiness was removed from the sidebar, app route, Today-board metric, continuity card, operational-signal prompt and routine report catalogue. The command board now shows on-call cover plus open handovers and active issues instead of asking colleagues for a separate pulse.
- **Historical data protection:** existing `ops_readiness` documents were deliberately not deleted. Their Firestore rule now permits reading existing records but blocks all creation, editing and deletion, preventing the retired feature from silently returning as a manual obligation.
